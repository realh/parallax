<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="realh.co.uk.parallax3d" >

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <activity android:name="uk.co.realh.parallax.LauncherActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    <activity android:name="uk.co.realh.parallax.CubeTextureActivity" />

    <activity android:name="uk.co.realh.parallax.GeometriesActivity" />

    <activity android:name="uk.co.realh.parallax.ParametricGeometryActivity" />

    <activity android:name="uk.co.realh.parallax.MovingVerticesActivity" />

    <activity android:name="uk.co.realh.parallax.BufferGeometryActivity" />

    <activity android:name="uk.co.realh.parallax.BufferedParticlesActivity" />

    <activity android:name="uk.co.realh.parallax.BumpMappingActivity" />

    <!--
    <activity android:name="uk.co.realh.parallax.SkinMaterialActivity" />
    -->

    </application>

</manifest>
